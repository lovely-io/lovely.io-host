.package
  %header
    = link_to raw("#{highlight(package.name, params[:search])} - #{package.version.number}"), package
    = "(#{package.updated_at.to_s(:short)})"

  %article
    = package.description