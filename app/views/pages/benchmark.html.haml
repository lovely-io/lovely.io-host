%h1 DOM Speed Benchmark

%p
  <b>Disclamer</b> this benchmark uses the #{link_to "kick-ass package", package_path("kick-ass")}
  to test the library's performance. All source code at the package's GitHub repo.

%p
  #start.lui-button Start The Test!

%p
  #test-container

%p.white-space

:javascript
  Lovely(["kick-ass", "dom", "ui"], function(KickAss, $) {
    $(function() {
      var test = new KickAss({
        libs: [
          'lovely-1.4.3',
          'jquery-1.9.1',
          'jquery-2.0.0'
        ]
      });

      test.insertTo('#test-container');

      test.on('finish', function() {
        $('#start').hide();
      });

      $('#start').on('click', function() {
        this.hasClass('lui-button') && test.start();
        this.removeClass('lui-button').html('Working...');
      });
    });
  });

